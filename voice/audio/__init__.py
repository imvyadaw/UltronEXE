"""Shared audio-format utilities (voice/audio/processor.py) used by the
wake word detector, and the Vosk/Whisper STT engines - conversion between
sr.AudioData, raw PCM bytes, and numpy float arrays, plus resampling."""
