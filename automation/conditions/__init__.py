"""Conditions package
===================
Building blocks for branching (if/else over system state) and
repetition (loops over a tool call) inside RPA scripts and workflows,
so a workflow step can be "run this tool 5 times" or "only run this
tool if that app is running" without hand-writing Python.
"""
