# ui/voice_ui/avatar/assets/

Visual assets (sprites, Lottie/JSON animations, or video loops) for
each `AvatarState` in `../states.py` - one per state at minimum:
`idle`, `listening`, `thinking`, `speaking`, `error`.

Empty for now; no renderer consumes this folder yet. Keeping it out
of `storage/` since these are static app assets, not user data or
generated files.
