"""
ui.voice_ui - visual companion for the voice pipeline (wake word +
STT/TTS, config under `voice:` in config/default.yaml). Currently just
ui.voice_ui.avatar, a state machine other UI surfaces (or a future
desktop overlay) can render against.
"""
