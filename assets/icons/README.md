# assets/icons/

App/tray/UI icons (`.ico`, `.png`, `.svg`). Consumed by `ui/tray/`,
`ui/overlay/`, and `ui/orb/` for window/tray iconography. Keep at
least a 256x256 `.ico` for Windows tray use (see `ui/tray/`) and an
SVG or high-res PNG source alongside any rasterized export.
