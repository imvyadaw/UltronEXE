# assets/images/

Static images used by the UI layer (backgrounds, avatar states,
splash screens) - e.g. `ui/orb/`'s avatar-state art referenced in
`testing/unit/test_avatar_states.py`. Not for user-generated content;
that belongs under `storage/exports/` or `storage/cache/`.
