# assets/fonts/

Bundled fonts (`.ttf`/`.otf`) for `ui/` surfaces that shouldn't
depend on whatever's installed on the host OS (overlay, orb, web_ui).
Include the font's license file alongside it when adding one.
